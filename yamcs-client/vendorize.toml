target = "src/yamcs/protobuf/_vendor"

# Protobuf 4 is very different: https://protobuf.dev/news/2022-05-06/
# We should only run with it, once generating with protoc 3.19
packages = ["protobuf>=3.11,<4"]
